apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python app that return api data using Flask, named ${{values.app_name}}
  # Example for optional annotations
  annotations:
    github.com/project-slug: minhpham95/${{values.app_name}}
spec:
  type: service
  owner: development
  lifecycle: experimental