apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python app - ${{values.app_name}} that return api data using Flask
  # Example for optional annotations
  annotations:
    github.com/project-slug: minhpham95/${{values.app_name}}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental
